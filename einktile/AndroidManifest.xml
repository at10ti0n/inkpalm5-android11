<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="net.inkpalm.einktile" android:sharedUserId="android.uid.system" android:versionCode="6" android:versionName="6.0">
  <uses-permission android:name="android.permission.SET_WALLPAPER"/>
  <uses-permission android:name="android.permission.SET_WALLPAPER_HINTS"/>
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="27"/>
  <application android:label="E-Ink Mode" android:icon="@drawable/ic_tile">
    <service android:name=".ModeTile" android:label="E-Ink Mode" android:icon="@drawable/ic_tile"
             android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter><action android:name="android.service.quicksettings.action.QS_TILE"/></intent-filter>
    </service>
    <service android:name=".RefreshTile" android:label="Refresh Screen" android:icon="@drawable/ic_refresh"
             android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter><action android:name="android.service.quicksettings.action.QS_TILE"/></intent-filter>
    </service>
    <!-- Screen Temperature: Night Light drives the warm LED bank (replaces the Warmth tile, v6) -->
    <service android:name=".ScreenTempService" android:exported="false"/>
    <receiver android:name=".BootReceiver" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
      </intent-filter>
    </receiver>
    <service android:name=".RotationTile" android:label="Orientation" android:icon="@drawable/ic_portrait"
             android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter><action android:name="android.service.quicksettings.action.QS_TILE"/></intent-filter>
    </service>
    <!-- static standby image: sets/clears the lock-screen wallpaper from a file (no shell
         command exists for this on Android 11). See LockWallpaperReceiver.java -->
    <receiver android:name=".LockWallpaperReceiver" android:exported="true">
      <intent-filter>
        <action android:name="net.inkpalm.einktile.SET_LOCK_WALLPAPER"/>
        <action android:name="net.inkpalm.einktile.CLEAR_LOCK_WALLPAPER"/>
      </intent-filter>
    </receiver>
    <!-- stock Moaan apps and the stock SystemUI tile broadcast this for a full refresh -->
    <receiver android:name=".RefreshReceiver" android:exported="true">
      <intent-filter><action android:name="android.eink.force.refresh"/></intent-filter>
    </receiver>
  </application>
</manifest>
